<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="EstudosAvancadosXamarin.Views.ProductDescriptionPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Name, StringFormat='Product Name: {0}'}"/>
            <Label Text="{Binding Price, StringFormat='Price: {0} R$'}"/>
            <Label Text="{Binding Quantity, StringFormat='Quantity: {0}'}"/>
            <Button Text="Add to Cart" Command="{Binding AddProductToCartCommand}" VerticalOptions="EndAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>