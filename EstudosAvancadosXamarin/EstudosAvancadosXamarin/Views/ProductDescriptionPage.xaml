<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="EstudosAvancadosXamarin.Views.ProductDescriptionPage"
             Visual="Material">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="{Binding Name, StringFormat='Produto.: {0}'}"/>
            <Label Text="{Binding Price, StringFormat='Preço.: {0} R$'}"/>
            <Label Text="{Binding Quantity, StringFormat='Estoque.: {0}'}"/>
            <Entry Text="{Binding ItemQuantity}" Placeholder="Quantidade" BackgroundColor="Transparent"/>
            <Button Text="Adicionar ao carrinho" Command="{Binding AddProductToCartCommand}" VerticalOptions="EndAndExpand"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>